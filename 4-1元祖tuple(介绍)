-语法
a=(元素1,元素2)
-元祖的特性
1.是序列表，有序的
2.元祖数据值可以访问，不可修改(内容的不可修改)
  ex：t1=(1,2,3) 
      t1[1]=100==>报错
      t2=(2,3)
      t1=t1+t2==>(1,2,2,3,3)
3.元祖数据可以是任意类型
4.list所有特性除了可修改以外，元祖都具备（例如索引，分片(::),(:)含义不同，序列相加 相乘）
5.遍历元祖嵌套
 ex：t=((1,2,3),(liu xiao kang))
     for i in t:
         print(i)==>(1,2,3),(liu xiao kang)
     for i j in t:
         print(i---j)==>1 2 3---liuxiaokang
     
